# Plan 단계 독립 WIP

## 기본 정보
- **WorkID**: WIP-20260426-001
- **스테이지**: Plan
- **담당 에이전트**: analyst
- **생성일**: 2026-04-26
- **상태**: 진행 중

---

## 🔒 절대 규칙 준수 확인 (강제)

### analyst 절대 규칙 체크
- [x] 무조건 캐스팅 (Type)cast 남용하지 않음
- [x] 불필요한 dynamic 사용하지 않음
- [x] 빈 catch 블록이 없음
- [x] IsBusy 상태가 try-finally로 보장됨
- [x] 빈 catch 블록 없음
- [x] 테스트 삭제하지 않음
- [x] 커밋 없이 파일 수정하지 않음 (사용자 요청 전)
- [x] 단언 없이 추측하지 않음
- [x] 개발 파이프라인 지침을 파괴하지 않음
- [x] 모든 위반이 없음을 확인함

---

## 지시 정보

### 작업 대상 (Target)
SchemaEditor의 Field Configurations에 Enum 타입 입력 기능을 추가하고, Export(Binary/JSON) 및 코드 생성 파이프라인이 enum 필드를 일관되게 처리하도록 수정.

### 변경 파일 (Files)
1. `ExcelBinder/Models/ProjectConstants.cs` — enum 마커 상수 + 정수형 타입 화이트리스트 추가
2. `ExcelBinder/Services/TypeParser.cs` — TypeInfo에 IsEnum/EnumType 필드 추가, `:enum:` 토큰 파싱
3. `ExcelBinder/ViewModels/SchemaEditorViewModel.cs` — SchemaFieldViewModel에 EnumType, CanInputEnum 추가, Save/Load 토큰 인코딩
4. `ExcelBinder/Views/SchemaEditorView.xaml` — Enum Type 컬럼 추가 (정수형일 때만 활성), 컬럼 너비 재배치
5. `ExcelBinder/Services/ExportService.cs` — IsEnum 분기 추가 (Binary/JSON 모두 문자열로 출력)
6. `ExcelBinder/Services/CodeGeneratorService.cs` — enum 분기 추가 (`System.Enum.Parse<T>(reader.ReadString())` 인라인)

### 필수 조건 (Requirements)
- **Type Mappings UI/모델 변경 금지** (사용자 결정)
- **하위 호환 유지**: 기존 Schema JSON(`"int"`, `"List<int>"`)이 그대로 동작해야 함
- **Enum 입력 제약**: Data Type이 정수형(byte/short/int/uint/long/ulong)일 때만 Enum Type 입력 허용
- **Export 정책**: enum 컬럼은 엑셀 셀 값을 **문자열 그대로** 출력 (Binary/JSON 동일)
- **코드 생성 정책**: `System.Enum.Parse<T>(reader.ReadString())` 인라인 사용, 대소문자 구분 (기본형)
- **TypeMappings**: `Dictionary<string, string>` 모델·UI 무변경
- **ExportService.ExportToBinary 시그니처**: 변경 금지 (호출부 영향 회피)
- 절대 규칙 준수: 빈 catch 금지, 무조건 캐스팅 금지, dynamic 최소화

---

## 작업 진행 내용

### 1단계: 확인 및 분석 (Confirm & Analyze)
- [x] 지시 내용 확인 — 사용자와 6라운드 합의 완료
- [x] 관련 파일 목록 파악 — 6개 파일 식별
- [x] 기존 코드/문서 분석 — 사전 조사에서 ProjectConstants, TypeParser, SchemaEditorViewModel, SchemaEditorView, ExportService, CodeGeneratorService 모두 확인
- [x] 유형 판단 — 수정 (기존 SchemaEditor·Export·CodeGen 파이프라인 확장)
- [x] 영향 범위 분석 — 아래 영향 분석 참조

### 2단계: 계획 (Plan)
- [x] 작업 계획 수립
- [x] 예상 변경 파일 정리
- [x] 위험 요소 식별
- [x] 사용자 확인 요청 준비

### 3단계: 실행 (Execute)
- [x] 계획 문서화
- [x] 수렴 분석 — 보완 사항 도출
- [x] 보완 사항 분류 (필수/선택)
- [x] 수렴 확인: 필수 보완 사항 0건 달성
- [x] 잔존 선택 보완 사항 기록
- [ ] 사용자 확인 요청
- [ ] 사용자 응답 기록
- [ ] Gate 준비

---

## 진척도
- **진척도**: 75% (사용자 Plan 승인 대기)

---

## 크로스체크 결과

### architect 크로스체크 (Gate-1)
- [ ] 1차 검증 통과
- [ ] 2차 검증 통과
- [ ] 크로스체크 통과
- 상태: 대기 중 (Plan 승인 후 진행)

---

## 분석 결과

### 유형 판단
- **유형**: 수정
- **근거**: 기존 SchemaEditor의 Field Configurations 컬럼 추가, 기존 Export/CodeGen 파이프라인에 enum 분기 삽입. 새로운 Feature 카테고리나 신규 서비스 추가 없음.

### 영향 분석

| 파일 | 변경 유형 | 영향 |
|------|----------|------|
| `Models/ProjectConstants.cs` | 상수 추가 | 신규 상수만 추가, 기존 상수 무변경 |
| `Services/TypeParser.cs` | TypeInfo 필드 추가 + 파싱 분기 | TypeInfo 사용처(ExportService, CodeGeneratorService) 모두 검토 필요 |
| `ViewModels/SchemaEditorViewModel.cs` | SchemaFieldViewModel 속성 추가, Save/Load 변경 | 기존 Schema JSON 로드 시 EnumType 빈 문자열로 초기화 → 하위 호환 OK |
| `Views/SchemaEditorView.xaml` | 컬럼 추가, 너비 재배치 | UI 레이아웃 변경, 다른 화면 영향 없음 |
| `Services/ExportService.cs` | WritePrimitive/ParseField에 IsEnum 분기 | 기존 시그니처 변경 시 StaticDataProcessor 호출부 영향 → 시그니처 유지 |
| `Services/CodeGeneratorService.cs` | ConvertPrimitive/GetReadMethod 시그니처 변경 (string→TypeInfo) | 호출부 PrepareDataContext 1곳만 수정 |

- **위험도**: 중간
  - Schema JSON 인코딩 규칙 확장(`:enum:` 토큰)으로 TypeParser 파싱 순서 검증 필요 (기존 `:ref:` 와 공존)
  - CodeGeneratorService 시그니처 변경 — 같은 파일 내부 호출만 영향, 외부 영향 없음

### Schema JSON 인코딩 규칙 (확정)
- 일반: `"Element": "int"`
- Enum: `"Element": "int:enum:MyEnum"`
- List<enum>: `"Effects": "List<int:enum:MyEffect>"`
- Reference 병행: `"Element": "int:enum:MyEnum:ref:OtherTable"` (이론적; 실사용 미확인)

### 수렴 분석

**1회차 보완 사항 도출:**

| # | 보완 사항 | 분류 | 반영 여부 |
|---|----------|------|----------|
| 1 | TypeParser의 토큰 처리에서 `enum`과 `ref` 마커가 같은 `parts.Contains` 검사로 처리되므로, `enum` 토큰 다음에 오는 `EnumType`이 `ColumnName` fallback 로직(라인 43-47)에 잘못 잡힐 수 있음 | 필수 | 반영: ParseType 수정 시 `info.EnumType != null && last == info.EnumType` 조건도 ColumnName fallback에서 제외 |
| 2 | `SchemaFieldViewModel.SelectedType` 변경 시 `CanInputEnum` 알림 누락하면 UI가 비활성 상태 유지 | 필수 | 반영: `SelectedType` setter에서 `OnPropertyChanged(nameof(CanInputEnum))` + `EnumType=""` 초기화 |
| 3 | `SchemaEditorView.xaml`의 헤더 그리드 너비와 데이터 그리드 너비가 동일해야 정렬 유지 | 필수 | 반영: 양쪽 모두 (60/180/120/150/*) 동일 적용 |
| 4 | `ExportService.WritePrimitive` 시그니처를 `(string type, ...)` → `(TypeInfo info, ...)` 변경하면 caller도 변경 필요 | 필수 | 반영: 시그니처 그대로 유지하되, WriteField에서 IsEnum 분기 처리 |
| 5 | `CodeGeneratorService.GetReadMethod` 시그니처 변경 시 영향 | 필수 | 반영: 오버로드 추가(`GetReadMethod(TypeInfo)`) 또는 PrepareDataContext에서 분기 |
| 6 | List<enum> 컬럼이 여러 셀로 분산될 때 each 셀에 enum 이름이 들어감 → 기존 List 분기로 자연 처리됨 | 선택 | 검증만 필요, 코드 추가 불필요 |
| 7 | EnumType 입력란에 공백 문자열 검증 (`Trim()`) | 선택 | Save 시 `string.IsNullOrWhiteSpace` 체크 후 토큰 추가 |
| 8 | Enum Type 입력 필드 placeholder 텍스트 (`"int/byte/long 등 정수형에서만 사용"`) | 선택 | XAML `Tag` 속성으로 제공 |

**필수 보완 사항: 5건 → 모두 반영 (반복 1회로 0건 달성)**

**잔존 선택 보완 사항:**
- (선택-6) List<enum> 동작 검증은 Test 단계에서 수행
- (선택-7) EnumType 공백 검증 — Code 단계에서 적용
- (선택-8) Placeholder 텍스트 — Code 단계에서 적용

### 구현 세부 명세

#### 1. ProjectConstants.cs
```csharp
public static class Excel
{
    // ... 기존 ...
    public const string EnumMarker = "enum";
}

public static class Types
{
    // ... 기존 ...
    public static readonly string[] EnumUnderlyingTypes =
        { Byte, Short, Int, UInt, Long, ULong };
}
```

#### 2. TypeParser.cs
```csharp
public struct TypeInfo
{
    public string BaseType;
    public bool IsList;
    public bool IsReference;
    public string? RefType;
    public bool IsEnum;       // 신규
    public string? EnumType;  // 신규
    public string ColumnName;
}
```
파싱 로직: `parts.Contains("enum")` → `IsEnum = true`, 다음 토큰을 `EnumType`으로. ColumnName fallback에서 `EnumType`도 제외 대상에 추가.

#### 3. SchemaEditorViewModel.cs (SchemaFieldViewModel)
```csharp
private string _enumType = string.Empty;
public string EnumType { get => _enumType; set => SetProperty(ref _enumType, value); }

public bool CanInputEnum =>
    ProjectConstants.Types.EnumUnderlyingTypes.Contains(_selectedType);

// SelectedType setter 변경
public string SelectedType
{
    get => _selectedType;
    set
    {
        if (!SetProperty(ref _selectedType, value)) return;
        OnPropertyChanged(nameof(CanInputEnum));
        if (!CanInputEnum) EnumType = string.Empty;
    }
}
```

Save() 변경:
```csharp
string typeStr = field.SelectedType;
if (!string.IsNullOrWhiteSpace(field.EnumType))
    typeStr = $"{typeStr}:enum:{field.EnumType.Trim()}";
if (!string.IsNullOrWhiteSpace(field.ReferenceType))
    typeStr = $"{typeStr}:ref:{field.ReferenceType}";
if (field.Count > 1)
    typeStr = $"List<{typeStr}>";
schema.Fields[field.HeaderName] = typeStr;
```

Load 시 (생성자 내부):
```csharp
if (existingSchema != null && existingSchema.Fields.TryGetValue(group.Name, out var typeStr))
{
    var info = TypeParser.ParseType(typeStr, group.Name);
    selectedType = info.BaseType;
    referenceType = info.RefType ?? "";
    enumType = info.EnumType ?? "";
}
// new SchemaFieldViewModel { ..., EnumType = enumType, ... }
```

#### 4. SchemaEditorView.xaml
헤더 + 데이터 행 컬럼 정의:
```xml
<Grid.ColumnDefinitions>
    <ColumnDefinition Width="60"/>   <!-- Include -->
    <ColumnDefinition Width="180"/>  <!-- Excel Header -->
    <ColumnDefinition Width="120"/>  <!-- Data Type -->
    <ColumnDefinition Width="150"/>  <!-- Enum Type -->
    <ColumnDefinition Width="*"/>    <!-- Reference -->
</Grid.ColumnDefinitions>
```
Enum Type TextBox:
```xml
<TextBox Grid.Column="3"
         Text="{Binding EnumType, UpdateSourceTrigger=PropertyChanged}"
         IsEnabled="{Binding CanInputEnum}"
         Margin="5,0" Height="30"
         Tag="Enum (정수형 타입에서만 사용)"/>
```

#### 5. ExportService.cs
`WriteField` 변경 (라인 70~):
```csharp
private void WriteField(BinaryWriter writer, string type, string[] row, string[] header,
                       Dictionary<string, int> headerMap, string fieldName, FeatureDefinition feature)
{
    var info = TypeParser.ParseType(type, fieldName);
    if (info.IsList)
    {
        var indices = Enumerable.Range(0, header.Length)
            .Where(i => string.Equals(header[i]?.Trim(), info.ColumnName, StringComparison.OrdinalIgnoreCase))
            .ToList();
        writer.Write(indices.Count);
        foreach (var idx in indices)
            WritePrimitiveOrEnum(writer, info, row[idx]);
    }
    else
    {
        int idx = GetColumnIndex(headerMap, info.ColumnName, fieldName);
        string value = idx < row.Length ? row[idx] : "";
        WritePrimitiveOrEnum(writer, info, value);
    }
}

private void WritePrimitiveOrEnum(BinaryWriter writer, TypeInfo info, string value)
{
    if (info.IsEnum) { writer.Write(value ?? ""); return; }
    WritePrimitive(writer, info.BaseType, value);
}
```

`ParseField` 변경 (라인 143~):
```csharp
if (info.IsEnum)
    return value ?? "";
// 기존 로직 유지
```

#### 6. CodeGeneratorService.cs
`PrepareDataContext` 변경:
```csharp
var fields = schema.Fields.Where(f => !schema.ExcludedFields.Contains(f.Key)).Select(f =>
{
    var info = TypeParser.ParseType(f.Value, f.Key);
    string baseType = ConvertPrimitive(info, feature);
    return new
    {
        Name = f.Key,
        Type = baseType,
        IsList = info.IsList,
        IsReference = info.IsReference,
        IsEnum = info.IsEnum,
        EnumType = info.EnumType,
        RefType = info.RefType,
        RefClassName = info.RefType != null ? info.RefType + "Data" : null,
        ReadMethod = GetReadMethod(info),
        LowerName = f.Key.ToLower()
    };
}).ToList();
```

`ConvertPrimitive` 시그니처 변경:
```csharp
private string ConvertPrimitive(TypeInfo info, FeatureDefinition feature)
{
    if (info.IsEnum && !string.IsNullOrEmpty(info.EnumType))
        return info.EnumType;

    string baseType = info.BaseType;
    if (feature.TypeMappings != null && feature.TypeMappings.TryGetValue(baseType, out var mapped))
        return mapped;

    return baseType switch { /* 기존 그대로 */ };
}
```

`GetReadMethod` 오버로드 추가:
```csharp
private string GetReadMethod(TypeInfo info)
{
    if (info.IsEnum && !string.IsNullOrEmpty(info.EnumType))
        return $"System.Enum.Parse<{info.EnumType}>(reader.ReadString())";
    return GetReadMethod(info.BaseType);  // 기존 메서드 그대로 호출
}
```

`GetKeyType` 변경:
```csharp
private string GetKeyType(SchemaDefinition schema, FeatureDefinition feature)
{
    if (schema.Fields.TryGetValue(schema.Key, out var type))
    {
        var info = TypeParser.ParseType(type, schema.Key);
        return ConvertPrimitive(info, feature);
    }
    return ProjectConstants.Types.Int;
}
```

---

## 완료/취소 정보

### 완료 시
- **완료일**: (Plan 단계 완료 후 기록)
- **최종 진척도**: 100%
- **커밋 해시**: (해당 시)

### 취소 시
- **취소일**:
- **취소 사유**:
- **진척도**:
