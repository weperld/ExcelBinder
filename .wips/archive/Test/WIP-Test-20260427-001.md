# Test 단계 독립 WIP

## 기본 정보
- **WorkID**: WIP-20260427-001
- **스테이지**: Test
- **담당 에이전트**: tester
- **생성일**: 2026-04-28
- **상태**: 완료

---

## 🔒 절대 규칙 준수 확인
- [x] 모든 항목 준수

---

## 검증 결과

### 1. 빌드 검증
```
dotnet build ExcelBinder/
→ 오류 0개 (경고 33개는 모두 Scriban NuGet 보안 경고, 본 작업 무관)
경과 시간: 2.15초
```
**결과**: ✅ 통과

### 2. 정적 시나리오 추론

| # | 시나리오 | 추론 결과 |
|---|----------|----------|
| 1 | 앱 첫 실행, _groups.json 부재 | LoadGroups() → 빈 리스트, RefreshGroups()가 "전체"만 추가 → 사이드바에 "전체"만 표시. 우측은 Features 전체 ✅ |
| 2 | LastSelectedGroupId가 삭제된 그룹 | RefreshGroups()의 `Groups.FirstOrDefault(g => g.Id == lastId) ?? allGroup` → fallback으로 "전체" ✅ |
| 3 | 그룹 추가 시 중복 이름 | ValidateGroupName이 중복 검출 → ShowMessage 경고 → 추가 안 됨 ✅ |
| 4 | 그룹 추가 시 "전체"라는 이름 | ValidateGroupName이 AllGroupName과 동일 검출 → 거부 ✅ |
| 5 | 그룹 삭제 시 SelectedGroup이 삭제 대상 | DeleteGroup 후 `Groups.FirstOrDefault()` → "전체"로 fallback ✅ |
| 6 | Feature 우클릭 후 그룹 추가/제거 토글 | ContextMenu Opened가 매번 vm.CustomGroups를 순회하여 IsChecked 동적 설정. ToggleCommand 실행 시 즉시 SaveGroups + FilteredFeatures OnPropertyChanged ✅ |
| 7 | "전체" 그룹 우클릭 시 이름변경/삭제 | ListBoxItem ContextMenu의 IsEnabled가 InverseBooleanConverter로 IsAllGroup 반전 → 비활성 ✅ |
| 8 | _groups.json 손상 시 | LoadGroups() try-catch → 빈 리스트 + Warning 로그 ✅ |
| 9 | Feature 파일 삭제 → RefreshFeatures | 끝에 CleanupDanglingIds 호출 → dangling ID 제거 + SaveGroups ✅ |
| 10 | Settings.FeatureDefinitionsPath 변경 | OnSettingsPropertyChanged에서 _groupService.UpdateFeaturesDirectory + RefreshGroups 재호출 ✅ |

### 3. 회귀 위험 분석

| 영역 | 회귀 위험 | 평가 |
|------|----------|------|
| 기존 Features 컬렉션 사용처 | DashboardView가 Features → FilteredFeatures로 바인딩 변경. 기타 ViewModel은 Features 그대로 참조 | ✅ 무영향 |
| Settings JSON 하위 호환 | LastSelectedGroupId 누락 시 기본값 AllGroupId, BoundFeatures/CheckForUpdatesOnStartup 등 기존 속성 그대로 | ✅ 무영향 |
| FeatureService | 무변경 | ✅ 무영향 |
| 기존 ContextMenu 패턴 | DashboardView에는 기존 ContextMenu 없음, 신규만 추가 | ✅ 무영향 |
| ContextMenu 동적 빌드 | 코드비하인드 1개 핸들러로 한정, 다른 화면 영향 0 | ✅ 무영향 |
| MVVM 순수성 | 이 1개 예외만 허용 (Design Gate-2 승인 영역) | ⚠️ 정당화됨 |

### 4. 잠재 이슈 / 권고

| # | 항목 | 영향 | 권고 |
|---|------|------|------|
| 1 | "전체" 그룹 우클릭 시 ContextMenu가 열림 (메뉴 항목은 비활성) | UX 약간 어색 | 추후 검토 (Design 선택 보완 #3) |
| 2 | Feature ContextMenu가 그룹 0개일 때 "(그룹이 없습니다)" 표시 | 의도된 안내 | 그대로 유지 |
| 3 | 그룹명에 특수문자 무제한 허용 | JSON 직렬화는 문제없음, UI 깨짐 가능 | 운영자 책임 |
| 4 | _groups.json이 다른 프로세스에 잠긴 경우 | SaveGroups try-catch → Error 로그, 메모리 상태는 갱신됨 → 다음 저장 시도까지 disk-memory 불일치 | 추후 검토 |

---

## 진척도
- **진척도**: 100%

---

## 크로스체크 결과

### developer 크로스체크 (Gate-4)
- [x] 1차 검증 통과 — 빌드 0 오류, 정적 시나리오 10건 통과
- [x] 2차 검증 통과 — 회귀 위험 모두 무영향
- [x] 크로스체크 통과
- 상태: 통과

---

## 완료 정보
- **완료일**: 2026-04-28
- **최종 진척도**: 100%
