<Application x:Class="ExcelBinder.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ExcelBinder"
             xmlns:vm="clr-namespace:ExcelBinder.ViewModels"
             xmlns:items="clr-namespace:ExcelBinder.Views.ExecutionItems"
             xmlns:converters="clr-namespace:ExcelBinder.Converters"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- Data Templates for Execution ViewModels -->
        <DataTemplate DataType="{x:Type vm:StaticDataExecutionViewModel}">
            <items:StaticDataExecutionView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:LogicExecutionViewModel}">
            <items:LogicExecutionView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SchemaGenExecutionViewModel}">
            <items:SchemaGenExecutionView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:EnumExecutionViewModel}">
            <items:EnumExecutionView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ConstantsExecutionViewModel}">
            <items:ConstantsExecutionView/>
        </DataTemplate>

        <!-- Colors -->
        <Color x:Key="PrimaryColor">#007ACC</Color>
        <Color x:Key="SecondaryColor">#28A745</Color>
        <Color x:Key="WarningColor">#FFC107</Color>
        <Color x:Key="DangerColor">#DC3545</Color>
        <Color x:Key="BorderColor">#DEE2E6</Color>
        <Color x:Key="LightBackgroundColor">#F8F9FA</Color>
        <Color x:Key="DarkTextColor">#212529</Color>
        <Color x:Key="MutedTextColor">#6C757D</Color>

        <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}"/>
        <SolidColorBrush x:Key="SecondaryBrush" Color="{StaticResource SecondaryColor}"/>
        <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource WarningColor}"/>
        <SolidColorBrush x:Key="DangerBrush" Color="{StaticResource DangerColor}"/>
        <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}"/>
        <SolidColorBrush x:Key="LightBackgroundBrush" Color="{StaticResource LightBackgroundColor}"/>
        <SolidColorBrush x:Key="DarkTextBrush" Color="{StaticResource DarkTextColor}"/>
        <SolidColorBrush x:Key="MutedTextBrush" Color="{StaticResource MutedTextColor}"/>
        <SolidColorBrush x:Key="TagTextBrush" Color="#444444"/>

        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:InverseBooleanConverter x:Key="InverseBooleanConverter"/>

        <!-- Base Styles -->
        <Style TargetType="Button">
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource SecondaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="4"/>
                </Style>
            </Style.Resources>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="GroupBox">
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="0,10,0,15"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Border Grid.Row="0" Grid.RowSpan="2" 
                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="{TemplateBinding BorderThickness}" 
                                    CornerRadius="6" Background="White"/>
                            <Border Grid.Row="0" Margin="10,0,0,0" Padding="5,0" HorizontalAlignment="Left" Background="White" VerticalAlignment="Top">
                                <Border.RenderTransform>
                                    <TranslateTransform Y="-10"/>
                                </Border.RenderTransform>
                                <ContentPresenter ContentSource="Header" TextElement.FontWeight="Bold" TextElement.Foreground="{StaticResource PrimaryBrush}"/>
                            </Border>
                            <ContentPresenter Grid.Row="1" Margin="{TemplateBinding Padding}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="10" Color="Black" Opacity="0.05" ShadowDepth="2" Direction="270"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ToolTip Style with Fade In -->
        <Style x:Key="FadeInToolTip" TargetType="ToolTip">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HasDropShadow" Value="True"/>
            <Setter Property="Placement" Value="Top"/>
            <Setter Property="VerticalOffset" Value="-5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToolTip">
                        <Border x:Name="Border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="4" 
                                Opacity="0">
                            <ContentPresenter Margin="{TemplateBinding Padding}">
                                <ContentPresenter.Resources>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="TextWrapping" Value="Wrap"/>
                                        <Setter Property="MaxWidth" Value="400"/>
                                    </Style>
                                </ContentPresenter.Resources>
                            </ContentPresenter>
                        </Border>
                        <ControlTemplate.Triggers>
                            <EventTrigger RoutedEvent="Opened">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="Border" 
                                                         Storyboard.TargetProperty="Opacity" 
                                                         To="1" Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="HelpButtonStyle" TargetType="Button">
            <Style.Resources>
                <Style TargetType="ToolTip" BasedOn="{StaticResource FadeInToolTip}"/>
            </Style.Resources>
            <Setter Property="Width" Value="16"/>
            <Setter Property="Height" Value="16"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Cursor" Value="Help"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="ToolTipService.InitialShowDelay" Value="200"/>
            <Setter Property="ToolTipService.BetweenShowDelay" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid Background="Transparent">
                            <Ellipse x:Name="Circle" Fill="Transparent" Stroke="{StaticResource MutedTextBrush}" StrokeThickness="1"/>
                            <TextBlock Text="?" 
                                       Foreground="{StaticResource MutedTextBrush}" 
                                       FontSize="10" 
                                       FontWeight="Bold" 
                                       HorizontalAlignment="Center" 
                                       VerticalAlignment="Center"
                                       Margin="0,0,0,1"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Circle" Property="Fill" Value="{StaticResource PrimaryBrush}"/>
                                <Setter TargetName="Circle" Property="Stroke" Value="{StaticResource PrimaryBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Help Texts -->
        <System:String x:Key="FeatureIdHelp" xml:space="preserve" xmlns:System="clr-namespace:System;assembly=mscorlib">이 기능의 고유 식별자입니다.&#x0a;파일 이름이나 코드 생성 시 참조용으로 사용됩니다.</System:String>
        <System:String x:Key="CategoryHelp" xml:space="preserve" xmlns:System="clr-namespace:System;assembly=mscorlib">각 카테고리의 역할:&#x0a;- StaticData: 데이터를 JSON/Binary로 추출&#x0a;- Logic: 엑셀 수식을 코드로 변환&#x0a;- SchemaGen: 엑셀 구조로 스키마 생성&#x0a;- Enum: Definition 시트를 기반으로 C# Enum 생성&#x0a;- Constants: 엑셀 시트별로 상수를 담은 static partial class 생성</System:String>
        <System:String x:Key="NamespaceHelp" xml:space="preserve" xmlns:System="clr-namespace:System;assembly=mscorlib">생성되는 소스 코드 파일의 기본 네임스페이스입니다.&#x0a;일반적으로 프로젝트의 기본 네임스페이스와 일치시킵니다.</System:String>
        <System:String x:Key="ExcelPathHelp" xml:space="preserve" xmlns:System="clr-namespace:System;assembly=mscorlib">데이터를 읽어올 엑셀 파일들이 위치한 폴더입니다.&#x0a;하위 폴더의 파일들도 함께 스캔될 수 있습니다.</System:String>
        <System:String x:Key="SchemaPathHelp" xml:space="preserve" xmlns:System="clr-namespace:System;assembly=mscorlib">데이터 구조를 정의하는 스키마 파일이 저장될 폴더입니다.&#x0a;기존 스키마가 있다면 해당 폴더에서 로드합니다.</System:String>
        <System:String x:Key="ExportPathHelp" xml:space="preserve" xmlns:System="clr-namespace:System;assembly=mscorlib">최종 데이터(JSON 등)가 익스포트될 폴더입니다.&#x0a;빌드 파이프라인에서 참조하는 경로로 설정하세요.</System:String>
        <System:String x:Key="ScriptsPathHelp" xml:space="preserve" xmlns:System="clr-namespace:System;assembly=mscorlib">생성된 C# 소스 코드 파일들이 저장될 폴더입니다.&#x0a;프로젝트의 스크립트 폴더 경로로 설정하세요.</System:String>
        <System:String x:Key="KeyColumnHelp" xml:space="preserve" xmlns:System="clr-namespace:System;assembly=mscorlib">해당 시트에서 각 행을 구분하는 고유한 키(ID) 컬럼을 선택합니다.&#x0a;데이터 조회 시 인덱스로 사용됩니다.</System:String>
        <System:String x:Key="ReferenceTypeHelp" xml:space="preserve" xmlns:System="clr-namespace:System;assembly=mscorlib">다른 시트나 타입에 대한 참조가 필요한 경우 해당 타입 이름을 입력합니다.&#x0a;예: ItemData, StatusEffect 등</System:String>
        <System:String x:Key="PrimaryScanFolderHelp" xml:space="preserve" xmlns:System="clr-namespace:System;assembly=mscorlib">Feature 정의(.json) 파일들을 자동으로 검색할 메인 폴더입니다.&#x0a;이 폴더 내의 모든 설정 파일이 대시보드에 표시됩니다.</System:String>
        <System:String x:Key="AdditionalBoundSourcesHelp" xml:space="preserve" xmlns:System="clr-namespace:System;assembly=mscorlib">메인 폴더 외에 추가로 개별 파일이나 폴더를 지정하여&#x0a;Feature 정의를 불러올 수 있습니다.</System:String>
        <System:String x:Key="TemplatesConfigurationHelp" xml:space="preserve" xmlns:System="clr-namespace:System;assembly=mscorlib">코드 생성 시 사용할 Liquid 템플릿 파일의 경로입니다.&#x0a;AI Assistant를 통해 템플릿을 생성하거나 수정할 수 있습니다.</System:String>
        <System:String x:Key="GeneralInfoHelp" xml:space="preserve" xmlns:System="clr-namespace:System;assembly=mscorlib">Feature의 기본적인 식별 정보와 표시 이름을 설정합니다.&#x0a;Category에 따라 사용 가능한 설정 항목이 달라질 수 있습니다.</System:String>
        <System:String x:Key="FilePathsHelp" xml:space="preserve" xmlns:System="clr-namespace:System;assembly=mscorlib">엑셀 파일, 스키마, 결과물 등이 저장될 경로들을 설정합니다.&#x0a;상대 경로 혹은 절대 경로 모두 사용 가능합니다.</System:String>
        <System:String x:Key="TypeMappingsHelp" xml:space="preserve" xmlns:System="clr-namespace:System;assembly=mscorlib">엑셀에서 정의한 타입 이름을 C# 프로젝트에서 사용하는 실제 타입으로 매핑합니다.&#x0a;예를 들어, 'string'을 'string'으로 매핑할 수 있습니다.</System:String>
        <System:String x:Key="OutputOptionsHelp" xml:space="preserve" xmlns:System="clr-namespace:System;assembly=mscorlib">익스포트 시 결과 파일의 확장자와 지원하는 데이터 포맷(Binary/JSON)을 설정합니다.</System:String>
    </Application.Resources>
</Application>
