<FlowDocument xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              FontFamily="Segoe UI" FontSize="14" PagePadding="0" LineHeight="22">
    <Paragraph FontSize="28" FontWeight="Bold" Foreground="#2F4F8F" Margin="0,0,0,8">
        Schema Editor
    </Paragraph>
    <Paragraph Foreground="#555" Margin="0,0,0,18">
        Schema는 엑셀 헤더가 어떤 C# 타입으로 매핑되는지 정의합니다. SchemaEditor에서 시트별로 필드 포함 여부, 타입, 참조, Enum 타입을 지정합니다.
    </Paragraph>

    <Paragraph FontStyle="Italic" Foreground="Gray" Margin="0,0,0,16">
        ※ 상세 콘텐츠는 준비 중입니다. 화면 내 ❓ 버튼의 툴팁에서 각 항목 설명을 확인할 수 있습니다.
    </Paragraph>

    <Paragraph FontSize="20" FontWeight="SemiBold" Margin="0,8,0,4">화면 구성</Paragraph>
    <List MarkerStyle="Disc" Margin="20,0,0,12">
        <ListItem><Paragraph><Bold>좌측 탭</Bold> — 엑셀 시트 단위로 분리. 각 시트가 하나의 Schema 파일이 됩니다.</Paragraph></ListItem>
        <ListItem><Paragraph><Bold>Key Column</Bold> — 시트의 주요 식별자(Primary Key). 자동 포함되며 체크박스가 잠깁니다.</Paragraph></ListItem>
        <ListItem><Paragraph><Bold>Field 행</Bold> — Include / Excel Header / Data Type / Enum Type / Reference Type</Paragraph></ListItem>
    </List>

    <Paragraph FontSize="20" FontWeight="SemiBold" Margin="0,12,0,4">필드 설정 규칙</Paragraph>
    <List MarkerStyle="Disc" Margin="20,0,0,12">
        <ListItem><Paragraph><Bold>Include 체크</Bold> — 체크 해제된 필드는 스키마/추출 모두에서 제외</Paragraph></ListItem>
        <ListItem><Paragraph><Bold>Data Type = enum</Bold>일 때만 Enum Type 입력란이 활성화됨</Paragraph></ListItem>
        <ListItem><Paragraph>Enum 셀 값은 enum 멤버 이름 그대로 작성 (예: <Italic>Fire</Italic>) — <Bold>대소문자 구분</Bold></Paragraph></ListItem>
        <ListItem><Paragraph>List 컬럼은 <Bold>List&lt;type&gt;</Bold> 형식 자동 인식</Paragraph></ListItem>
        <ListItem><Paragraph>Reference Type은 다른 테이블의 키를 참조할 때 사용 (선택 사항)</Paragraph></ListItem>
    </List>

    <Paragraph FontSize="20" FontWeight="SemiBold" Margin="0,12,0,4">엑셀 헤더 규칙</Paragraph>
    <Paragraph Margin="0,0,0,8">
        엑셀 헤더에 <Bold>#</Bold> 접두어가 붙은 열은 SchemaEditor에 표시되지 않으며 추출에서도 제외됩니다.
        주석/설명용 열에 사용하세요.
    </Paragraph>
</FlowDocument>
