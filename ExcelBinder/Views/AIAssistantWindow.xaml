<Window x:Class="ExcelBinder.Views.AIAssistantWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AI Template Assistant" Height="650" Width="1000" WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left: Chat UI -->
        <Grid Grid.Column="0" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <GroupBox Header="AI Conversation">
                <ItemsControl ItemsSource="{Binding ChatHistory}">
                    <ItemsControl.Template>
                        <ControlTemplate>
                            <ScrollViewer x:Name="ChatScrollViewer" VerticalScrollBarVisibility="Auto">
                                <ItemsPresenter />
                            </ScrollViewer>
                        </ControlTemplate>
                    </ItemsControl.Template>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{Binding Background}" CornerRadius="10" Padding="10" Margin="5" HorizontalAlignment="{Binding Alignment}" MaxWidth="300">
                                <StackPanel>
                                    <TextBlock Text="{Binding Role}" FontSize="10" FontWeight="Bold" Margin="0,0,0,5"/>
                                    <TextBlock Text="{Binding Content}" TextWrapping="Wrap"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </GroupBox>

            <TextBox Grid.Row="1" Text="{Binding UserPrompt, UpdateSourceTrigger=PropertyChanged}" TextWrapping="Wrap" Height="80" Margin="0,10,0,0" Padding="5" AcceptsReturn="True">
                <TextBox.InputBindings>
                    <KeyBinding Key="Enter" Modifiers="Control" Command="{Binding SendCommand}"/>
                </TextBox.InputBindings>
            </TextBox>

            <Grid Grid.Row="2" Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" Foreground="#666"/>
                <Button Grid.Column="1" Content="Send (Ctrl+Enter)" Command="{Binding SendCommand}" Width="120" Height="30" IsEnabled="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                <!-- Note: Actually we need a proper InverseBooleanToBooleanConverter or similar, but for now we'll just keep it simple -->
            </Grid>
        </Grid>

        <!-- Right: Preview UI -->
        <Grid Grid.Column="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Generated Template Preview (.liquid)" FontWeight="Bold" Margin="0,0,0,10"/>
            <TextBox Grid.Row="1" Text="{Binding GeneratedTemplate}" FontFamily="Consolas" IsReadOnly="False" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"/>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Content="Apply to Feature" Command="{Binding ApplyCommand}" Width="150" Height="35" Background="#4CAF50" Foreground="White" FontWeight="Bold"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
