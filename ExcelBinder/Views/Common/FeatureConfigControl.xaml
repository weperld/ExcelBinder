<UserControl x:Class="ExcelBinder.Views.Common.FeatureConfigControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    <GroupBox Header="Feature Configuration">
        <Grid Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <TextBlock Text="Excel:" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,5,10,5"/>
            <TextBlock Grid.Column="1" Text="{Binding SelectedFeature.ExcelPath}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" Margin="0,5,20,5" Foreground="{StaticResource MutedTextBrush}"/>
            
            <TextBlock Grid.Row="0" Grid.Column="2" Text="Schema:" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,5,10,5" Visibility="{Binding IsSchemaPathVisible, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            <TextBlock Grid.Row="0" Grid.Column="3" Text="{Binding SelectedFeature.SchemaPath}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" Margin="0,5,0,5" Foreground="{StaticResource MutedTextBrush}" Visibility="{Binding IsSchemaPathVisible, Converter={StaticResource BooleanToVisibilityConverter}}"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Export:" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,5,10,5" Visibility="{Binding IsExportPathVisible, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding SelectedFeature.ExportPath}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" Margin="0,5,20,5" Foreground="{StaticResource MutedTextBrush}" Visibility="{Binding IsExportPathVisible, Converter={StaticResource BooleanToVisibilityConverter}}"/>

            <TextBlock Grid.Row="1" Grid.Column="2" Text="Scripts:" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,5,10,5" Visibility="{Binding IsScriptsPathVisible, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            <TextBlock Grid.Row="1" Grid.Column="3" Text="{Binding SelectedFeature.ScriptsPath}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" Margin="0,5,0,5" Foreground="{StaticResource MutedTextBrush}" Visibility="{Binding IsScriptsPathVisible, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </Grid>
    </GroupBox>
</UserControl>
