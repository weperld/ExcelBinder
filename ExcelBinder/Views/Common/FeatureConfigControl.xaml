<UserControl x:Class="ExcelBinder.Views.Common.FeatureConfigControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    <GroupBox Header="Feature Configuration">
        <WrapPanel Margin="5">
            <StackPanel Orientation="Horizontal" Margin="0,5,30,5">
                <TextBlock Text="Excel:" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock Text="{Binding SelectedFeature.ExcelPath}" TextTrimming="CharacterEllipsis" MaxWidth="300" VerticalAlignment="Center" Foreground="{StaticResource MutedTextBrush}"/>
            </StackPanel>
            
            <StackPanel Orientation="Horizontal" Margin="0,5,30,5" Visibility="{Binding IsSchemaPathVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                <TextBlock Text="Schema:" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock Text="{Binding SelectedFeature.SchemaPath}" TextTrimming="CharacterEllipsis" MaxWidth="300" VerticalAlignment="Center" Foreground="{StaticResource MutedTextBrush}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,5,30,5" Visibility="{Binding IsExportPathVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                <TextBlock Text="Export:" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock Text="{Binding SelectedFeature.ExportPath}" TextTrimming="CharacterEllipsis" MaxWidth="300" VerticalAlignment="Center" Foreground="{StaticResource MutedTextBrush}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,5,0,5" Visibility="{Binding IsScriptsPathVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                <TextBlock Text="Scripts:" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock Text="{Binding SelectedFeature.ScriptsPath}" TextTrimming="CharacterEllipsis" MaxWidth="300" VerticalAlignment="Center" Foreground="{StaticResource MutedTextBrush}"/>
            </StackPanel>
        </WrapPanel>
    </GroupBox>
</UserControl>
