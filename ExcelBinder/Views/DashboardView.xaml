<UserControl x:Class="ExcelBinder.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Available Features" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <ListBox Grid.Row="1" ItemsSource="{Binding Features}" BorderThickness="0">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel IsItemsHost="True"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="#DDD" BorderThickness="1" CornerRadius="8" Width="200" Height="180" Margin="10" Padding="15" Background="White">
                        <StackPanel>
                            <TextBlock Text="{Binding Name}" FontSize="18" FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,0,0,5"/>
                            <TextBlock Text="{Binding Description}" FontSize="12" Foreground="#666" TextWrapping="Wrap" Height="50"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                                <Button Content="Edit" Command="{Binding DataContext.EditFeatureCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Margin="0,0,5,0" Width="50"/>
                                <Button Content="Select" Command="{Binding DataContext.SelectFeatureCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Width="70" Background="#007ACC" Foreground="White" BorderThickness="0"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Create New Feature" Command="{Binding CreateFeatureCommand}" Width="150" Height="30" Margin="0,0,10,0" Background="#28A745" Foreground="White" FontWeight="Bold" BorderThickness="0"/>
            <Button Content="Settings" Command="{Binding NavigateToSettingsCommand}" Width="100" Height="30" Margin="0,0,10,0"/>
            <Button Content="Refresh Features" Command="{Binding RefreshFeaturesCommand}" Width="120" Height="30"/>
        </StackPanel>
    </Grid>
</UserControl>
