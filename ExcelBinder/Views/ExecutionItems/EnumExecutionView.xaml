<UserControl x:Class="ExcelBinder.Views.ExecutionItems.EnumExecutionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:common="clr-namespace:ExcelBinder.Views.Common"
             xmlns:items="clr-namespace:ExcelBinder.Views.ExecutionItems"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <common:FeatureHeaderControl Grid.Row="0" Margin="0,0,0,20"/>

        <!-- Config -->
        <common:FeatureConfigControl Grid.Row="1" Margin="0,0,0,20"/>

        <!-- Source Files (Enum Specialized) -->
        <items:EnumSourceFileListControl Grid.Row="2"/>

        <!-- Actions -->
        <Border Grid.Row="3" Background="{StaticResource LightBackgroundBrush}" Padding="20" Margin="0,20,0,0" CornerRadius="8">
            <DockPanel>
                <TextBlock Text="Generates C# Enum files from the 'Definition' sheet and value sheets." 
                           VerticalAlignment="Center" Foreground="{StaticResource MutedTextBrush}"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Create Template" Command="{Binding CreateTemplateCommand}" Width="140" Height="36" Margin="0,0,10,0"/>
                    <Button Content="Show Logs" Command="{Binding ShowLogsCommand}" Width="100" Height="36" Margin="0,0,10,0"/>
                    <Button Content="Generate Enum Code" Style="{StaticResource PrimaryButton}" 
                            Command="{Binding GenerateCommand}" Width="180" Height="36"/>
                </StackPanel>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
