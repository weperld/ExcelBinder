<UserControl x:Class="ExcelBinder.Views.ExecutionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <Button Content="← Back to Dashboard" Command="{Binding NavigateToDashboardCommand}" HorizontalAlignment="Left" Width="150" Height="25"/>
            <TextBlock Text="{Binding SelectedFeature.Name}" FontSize="24" FontWeight="Bold" Margin="0,10,0,0"/>
            <TextBlock Text="{Binding SelectedFeature.Description}" FontSize="14" Foreground="#666"/>
        </StackPanel>

        <GroupBox Grid.Row="1" Header="Paths Info" Margin="0,0,0,10">
            <Grid Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <TextBlock Text="Excel:" FontWeight="SemiBold" Width="60"/>
                <TextBlock Grid.Column="1" Text="{Binding SelectedFeature.ExcelPath}" TextTrimming="CharacterEllipsis"/>
                
                <TextBlock Grid.Row="1" Text="Schema:" FontWeight="SemiBold" Width="60" Visibility="{Binding IsSchemaPathVisible, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding SelectedFeature.SchemaPath}" TextTrimming="CharacterEllipsis" Visibility="{Binding IsSchemaPathVisible, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                <TextBlock Grid.Row="2" Text="Export:" FontWeight="SemiBold" Width="60" Visibility="{Binding IsExportPathVisible, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding SelectedFeature.ExportPath}" TextTrimming="CharacterEllipsis" Visibility="{Binding IsExportPathVisible, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                <TextBlock Grid.Row="3" Text="Scripts:" FontWeight="SemiBold" Width="60" Visibility="{Binding IsScriptsPathVisible, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding SelectedFeature.ScriptsPath}" TextTrimming="CharacterEllipsis" Visibility="{Binding IsScriptsPathVisible, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </Grid>
        </GroupBox>

        <GroupBox Grid.Row="2" Header="Select Files to Process">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <ListBox ItemsSource="{Binding ExcelFiles}" Margin="0,0,0,5">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                    <Button Content="All" Command="{Binding SelectAllCommand}" Width="35" Height="20" Margin="0,0,2,0" FontSize="10"/>
                                    <Button Content="None" Command="{Binding DeselectAllCommand}" Width="40" Height="20" Margin="0,0,5,0" FontSize="10"/>
                                    <TextBlock Text="{Binding FileName}" FontWeight="Bold" VerticalAlignment="Center"/>
                                </StackPanel>
                                <ItemsControl ItemsSource="{Binding Sheets}" Margin="25,5,0,10">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <DockPanel LastChildFill="True" Margin="0,1">
                                                <CheckBox IsChecked="{Binding IsSelected}" IsEnabled="{Binding CanBeSelected}" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                                <Ellipse Width="8" Height="8" Margin="0,0,5,0" VerticalAlignment="Center">
                                                    <Ellipse.Style>
                                                        <Style TargetType="Ellipse">
                                                            <Setter Property="Fill" Value="Green"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsSchemaFound}" Value="False">
                                                                    <Setter Property="Fill" Value="Red"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Ellipse.Style>
                                                </Ellipse>
                                                <TextBlock Text="{Binding SheetName}" Width="150"/>
                                                <TextBlock Text="{Binding SchemaPath}" FontSize="10" Foreground="#888" TextTrimming="CharacterEllipsis"/>
                                            </DockPanel>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <StackPanel Grid.Row="1" Orientation="Horizontal">
                    <Button Content="Select All" Command="{Binding SelectAllCommand}" Width="80" Margin="0,0,5,0"/>
                    <Button Content="Deselect All" Command="{Binding DeselectAllCommand}" Width="80"/>
                    <Button Content="Refresh Files" Command="{Binding RefreshFilesCommand}" Width="100" Margin="10,0,0,0"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <Grid Grid.Row="3" Margin="0,15,0,0">
            <StackPanel Orientation="Horizontal">
                <CheckBox Content="Binary" IsChecked="{Binding IsBinaryChecked}" Visibility="{Binding SelectedFeature.OutputOptions.SupportsBinary, Converter={StaticResource BooleanToVisibilityConverter}}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <CheckBox Content="JSON" IsChecked="{Binding IsJsonChecked}" Visibility="{Binding SelectedFeature.OutputOptions.SupportsJson, Converter={StaticResource BooleanToVisibilityConverter}}" VerticalAlignment="Center" Margin="0,0,10,0"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Command="{Binding GenerateCodeCommand}" Width="150" Height="40">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Content" Value="Generate Code Only"/>
                            <Setter Property="Margin" Value="0,0,10,0"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedFeature.Category}" Value="Logic">
                                    <Setter Property="Content" Value="Generate Logic"/>
                                    <Setter Property="Margin" Value="0"/>
                                    <Setter Property="Background" Value="#007ACC"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding SelectedFeature.Category}" Value="SchemaGen">
                                    <Setter Property="Content" Value="Generate Schemas"/>
                                    <Setter Property="Margin" Value="0"/>
                                    <Setter Property="Background" Value="#007ACC"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <Button Content="Execute Export" Command="{Binding ExportCommand}" Width="150" Height="40" Background="#007ACC" Foreground="White" FontWeight="Bold">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedFeature.Category}" Value="Logic">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding SelectedFeature.Category}" Value="SchemaGen">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
