<UserControl x:Class="ExcelBinder.Views.SchemaEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000" Background="White">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="0,5,0,25">
            <TextBlock Text="Schema Editor" FontSize="28" FontWeight="Bold" Padding="0,5,0,0" Foreground="{StaticResource PrimaryBrush}"/>
            <TextBlock Text="Configure how Excel headers map to data types and references" FontSize="14" Foreground="{StaticResource MutedTextBrush}" Margin="0,5,0,0"/>
        </StackPanel>

        <TabControl Grid.Row="1" ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem}" TabStripPlacement="Left" BorderThickness="1" BorderBrush="{StaticResource BorderBrush}">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Padding" Value="15,10"/>
                    <Setter Property="HeaderTemplate">
                        <Setter.Value>
                            <DataTemplate>
                                <TextBlock Text="{Binding SheetName}" FontSize="14" FontWeight="SemiBold"/>
                            </DataTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>
            <TabControl.ContentTemplate>
                <DataTemplate>
                    <Grid Margin="20,0,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Margin="0,0,0,20">
                            <TextBlock Text="{Binding DisplayName}" FontSize="20" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}"/>
                            <TextBlock Text="{Binding ExcelPath}" FontSize="12" Foreground="{StaticResource MutedTextBrush}" Margin="2,2,0,0"/>
                        </StackPanel>

                        <Border Grid.Row="1" Background="{StaticResource LightBackgroundBrush}" CornerRadius="6" Padding="15" Margin="0,0,0,20">
                            <StackPanel Orientation="Horizontal">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,15,0">
                                    <TextBlock Text="Key Column:" FontWeight="Bold"/>
                                    <Button Style="{StaticResource HelpButtonStyle}" ToolTipService.ToolTip="{StaticResource KeyColumnHelp}"/>
                                </StackPanel>
                                <ComboBox ItemsSource="{Binding Headers}" SelectedItem="{Binding SelectedKey}" Width="250" Height="32"/>
                                <TextBlock Text="(The primary identifier for this sheet)" VerticalAlignment="Center" Margin="15,0,0,0" FontSize="12" Foreground="{StaticResource MutedTextBrush}"/>
                            </StackPanel>
                        </Border>

                        <GroupBox Grid.Row="2" Header="Field Configurations">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                
                                <!-- Header Labels -->
                                <Grid Grid.Row="0" Margin="10,5,25,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition Width="200"/>
                                        <ColumnDefinition Width="150"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="Include" FontWeight="Bold" Foreground="{StaticResource MutedTextBrush}"/>
                                    <TextBlock Grid.Column="1" Text="Excel Header" FontWeight="Bold" Foreground="{StaticResource MutedTextBrush}"/>
                                    <TextBlock Grid.Column="2" Text="Data Type" FontWeight="Bold" Foreground="{StaticResource MutedTextBrush}"/>
                                    <StackPanel Grid.Column="3" Orientation="Horizontal">
                                        <TextBlock Text="Reference (Optional)" FontWeight="Bold" Foreground="{StaticResource MutedTextBrush}"/>
                                        <Button Style="{StaticResource HelpButtonStyle}" ToolTipService.ToolTip="{StaticResource ReferenceTypeHelp}"/>
                                    </StackPanel>
                                </Grid>
                                
                                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                                    <ItemsControl ItemsSource="{Binding Fields}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="5,8">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="60"/>
                                                            <ColumnDefinition Width="200"/>
                                                            <ColumnDefinition Width="150"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>
                                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                            <CheckBox IsChecked="{Binding IsIncluded}"
                                                                      IsEnabled="{Binding IsKeyColumn, Converter={StaticResource InverseBooleanConverter}}"
                                                                      Margin="5,0"/>
                                                            <TextBlock Text="&#x1F512;" FontSize="12"
                                                                       Visibility="{Binding IsKeyColumn, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                                       VerticalAlignment="Center" Margin="2,0,0,0"/>
                                                        </StackPanel>
                                                        <TextBlock Grid.Column="1" Text="{Binding DisplayName}" VerticalAlignment="Center" FontWeight="SemiBold" Margin="5,0"/>
                                                        <ComboBox Grid.Column="2" ItemsSource="{Binding DataContext.AvailableTypes, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                                  SelectedItem="{Binding SelectedType}" Margin="5,0" Height="30"/>
                                                        <TextBox Grid.Column="3" Text="{Binding ReferenceType, UpdateSourceTrigger=PropertyChanged}" Margin="5,0" Height="30"
                                                                 Tag="Reference Type (Optional)"/>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                            </Grid>
                        </GroupBox>
                    </Grid>
                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>

        <Border Grid.Row="2" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Padding="0,20,0,0" Margin="0,15,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" Command="{Binding CancelCommand}" Width="100" Height="38" Margin="0,0,12,0"/>
                <Button Content="Complete Schema" Style="{StaticResource SecondaryButton}" 
                        Command="{Binding CompleteCommand}" Width="180" Height="38"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
